<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberMapper">

<insert id="insertMember" parameterType="userIn">
	insert into MEMBER(user_no, user_id, password)
	values(SEQ_MEMBER.nextval, #{id}, #{pwd})
</insert>

<select id="selectMember" parameterType="UserOut" resultType="int">
	select count(*) from MEMBER
	where user_id = #{id} and password = #{pwd}
</select>

</mapper>
